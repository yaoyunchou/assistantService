# 服务器 WebSocket 文档（入口）

**预售订单 WebSocket 调用说明**已单独维护，请直接阅读：

→ **[`../预售订单-WebSocket调用说明.md`](../预售订单-WebSocket调用说明.md)**

该文档包含：`assistant_http` / `forward`、`POST /api/pinduoduo/erp-presell/collect`、超时、`orders` 字段与示例 JSON。

其它 ERP 能力与完整 Socket 协议见 [`../socketio-assistant-http.md`](../socketio-assistant-http.md)。
