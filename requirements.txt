# Web框架
flask>=2.3.0

# API 文档 (Swagger UI)
flasgger>=0.9.7

# 浏览器自动化
playwright>=1.40.0

# HTTP请求
requests>=2.31.0

# 打包工具
pyinstaller>=6.0.0

# 系统托盘
pystray>=0.19.0

# 图标处理（pystray依赖）
Pillow>=10.0.0

# 原生窗口（桌面应用窗口）
pywebview>=4.4.0

# 环境变量管理
python-dotenv>=1.0.0

# WebSocket / Socket.IO 客户端
websocket-client>=1.6.0
python-socketio[client]>=5.10.0

# 定时任务
APScheduler>=3.10.0
croniter>=2.0.0

# AI API（OpenAI 兼容接口，用于库存关联匹配等）
openai>=1.0.0

# TOML 配置文件读写（Python 3.11+ 自带 tomllib 读取，但写入仍需 tomli-w）
tomli>=2.0.0
tomli-w>=1.0.0

# Excel 读写
openpyxl>=3.1.0
